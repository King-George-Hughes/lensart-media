export interface BlogData {
  id: string;
  title: string;
  image?: string;
  description: string;
  //   createdAt: string;
}

export interface Blog {
  id: string;
  title: string;
  image?: string;
  description: string;
  createdAt?: string;
}
